// Placeholder: DDS ring buffer + ONNX Runtime session (plan §7.2).
#include <cstdio>

int main() {
  std::puts("thermal_infer_node: link unitree_sdk2 + CycloneDDS + feature buffer here");
  return 0;
}
